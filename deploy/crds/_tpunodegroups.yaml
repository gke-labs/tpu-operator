---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: tpunodegroups.
spec:
  group: ""
  names:
    kind: TPUNodeGroup
    listKind: TPUNodeGroupList
    plural: tpunodegroups
    singular: tpunodegroup
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: TPUNodeGroup is the Schema for the tpunodegroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TPUNodeGroupSpec defines the desired state of a TPUNodeGroup
            properties:
              acceleratorConnectionMode:
                description: |-
                  AcceleratorConnectionMode dictates how the chips are interconnected.
                  Currently, the only valid value is static. (Immutable)
                type: string
              bootstrapKubernetes:
                description: BootstrapKubernetes defines if and how the controller
                  should install K8s components.
                properties:
                  enabled:
                    description: |-
                      Enabled indicates if the controller should bootstrap the nodes.
                      If omitted, the controller will not bootstrap the nodes.
                    type: boolean
                  version:
                    description: Version is the Kubernetes version to install.
                    type: string
                type: object
              instanceConfig:
                description: |-
                  InstanceConfig allows the controller to generate an instance template.
                  Cannot be set if InstanceTemplate is provided.
                properties:
                  bootDiskSizeGB:
                    description: BootDiskSizeGB is the size of the boot disk in GB.
                    format: int32
                    type: integer
                  diskType:
                    description: DiskType specifies the type of the boot disk (e.g.,
                      "pd-ssd", "pd-balanced").
                    type: string
                  image:
                    description: Image is the boot disk image URI.
                    type: string
                  machineType:
                    description: MachineType is the GCE machine type (e.g., "tpu7x-standard-4t").
                    type: string
                  metadata:
                    additionalProperties:
                      type: string
                    description: Metadata allows setting custom GCE metadata.
                    type: object
                  networkTags:
                    description: NetworkTags are used to apply GCP firewall rules
                      to the TPU nodes.
                    items:
                      type: string
                    type: array
                  provisioningModel:
                    description: |-
                      ProvisioningModel specifies spot, reservation-bound
                      Defaults to reservation-bound if the reservation field is specified.
                      Defaults to on-demand if this field nor the reservation field is specified.
                    type: string
                  reservation:
                    description: |-
                      Reservation is the name of the reservation to consume.
                      Required if ProvisioningModel is "reservation-bound.
                    type: string
                  serviceAccount:
                    description: ServiceAccount is the GCP service account attached
                      to the VMs.
                    type: string
                  subnetwork:
                    description: Subnetwork is the VPC subnetwork URI.
                    type: string
                required:
                - machineType
                type: object
              instanceTemplate:
                description: |-
                  InstanceTemplate is the full URI of a user-provided instance template.
                  Cannot be set if InstanceConfig is provided.
                type: string
              minNodeCount:
                description: MinNodeCount is the minimum required for a single-host
                  slice.
                format: int32
                type: integer
              nodeCount:
                description: NodeCount is the total number of VMs desired.
                format: int32
                type: integer
              nodeLocation:
                description: NodeLocation is the GCE Zone where the nodes will be
                  provisioned.
                type: string
              project:
                description: Project is the GCP project ID.
                type: string
              topology:
                description: |-
                  Topology specifies the physical arrangement of the TPU chips.
                  Required for multi-host slices. If omitted, assumes single-host.
                type: string
            required:
            - acceleratorConnectionMode
            - nodeCount
            - nodeLocation
            - project
            type: object
          status:
            description: TPUNodeGroupStatus defines the observed state of a TPUNodeGroup
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
