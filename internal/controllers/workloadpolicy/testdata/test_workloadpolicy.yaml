apiVersion: tpu.google.com/v1alpha1
kind: WorkloadPolicy
metadata:
  name: test-workloadpolicy
  namespace: default
spec:
  project: ${E2E_PROJECT}
  region: ${E2E_REGION}
  acceleratorTopology: "2x2x1"
  type: HIGH_THROUGHPUT
