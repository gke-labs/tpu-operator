apiVersion: tpu.google.com/v1alpha1
kind: ManagedInstanceGroup
metadata:
  name: test-mig
spec:
  project: ${E2E_PROJECT}
  location: ${E2E_ZONE}
  instanceTemplate: https://www.googleapis.com/compute/v1/projects/${E2E_PROJECT}/global/instanceTemplates/test-multihost-template
  targetSize: 2
  workloadPolicy: https://www.googleapis.com/compute/v1/projects/${E2E_PROJECT}/regions/${E2E_REGION}/resourcePolicies/test-multihost-policy
